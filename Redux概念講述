REACT只提供輕量級的視圖層框架，在遇到資料傳遞的時候，
需要一直向上傳輸給父級組件直到最頂層，在遇到大型專案時，組件傳值會變得不可維護。

配套一個數據層框架可以解決數據傳值問題。REDUX要求將資料放在公共的存儲空間，組件並不放數據。
當組件修改時，只需改變store的數據，灰色的組件自動感知到store改變，重新刷新資料。

Redux=reducer+flux
Flux是REDUX的前身，flux 擁有很多存儲空間，因此會有數據依賴的問題。
